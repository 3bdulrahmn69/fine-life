'use client';

// This hook is now deprecated - use the global preferences provider instead
// Kept for backward compatibility with existing components

export {
  useUserPreferences,
  type UserPreferences,
} from '../providers/preferences-provider';
